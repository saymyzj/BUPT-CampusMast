# 阶段三 C-owner 本轮交付说明（面向 A/B 协作）

> 日期：2026-04-15
> 编写人：成员 C
> 目的：同步本轮已完成范围、当前边界和后续联调入口

> 历史说明：本文记录 2026-04-15 当时的阶段三交付口径，其中包含 Alembic、OSS、生产部署等历史方案。当前有效基线已调整为本地验收版，请以根目录 README、API 规范、数据库设计和系统架构设计为准。

---

## 1. 与上一次提交的差异（当前工作区对比）

本轮改动基于当前分支最近一次提交继续推进，属于“冻结后补齐 C 主责基础设施”的增量，不是重做 A/B 的业务主线。

差异摘要：

- 变更规模：35 个已跟踪文件被修改，新增多个基础设施文件（迁移、种子、工具、测试等）
- 主要增量集中在：
  - 后端 C 主责路由与服务从 stub 变为真实 SQLAlchemy 实现
  - API 契约中地图字段与 WebSocket v1 事件范围收口
  - 部署前准备（迁移、种子、生产模板、compose 启动链路）
  - C-owner 覆盖测试补充

备注：上一次提交本身主要是前端地图样式/结构的 Route A 重构，这一轮没有改写 A 的页面主责实现，而是补齐可联调后端底座。

---

## 2. 本轮已完成内容（仅 C 份内）

### 2.1 契约与冻结收口

- 更新 docs/API接口规范.yaml
- 地图楼宇字段统一为：latitude、longitude、polygon
- WebSocket v1 事件收敛到当前真实实现：
  - notification:{userId}
  - chat:{taskId}
  - presence:{taskId} 仅作预留，不作为 v1 联调阻塞项

### 2.2 后端基础设施落地

已将以下 C-owner 模块从占位实现改为可运行服务，并在 app 主入口挂载：

- 认证与鉴权：注册、登录、刷新、用户上下文依赖
- 通知服务：通知读取、推送基础能力
- 聊天 HTTP 兜底：会话读取、发送消息、已读同步基础链路
- 审核记录与复审入口：面向 moderation/admin 基础流程
- 后台配置与首页内容配置
- 上传签名（OSS 相关能力）
- 地图查询：楼宇列表与附近任务查询（当前支持联调结构）
- WebSocket 网关：连接鉴权与频道访问控制

### 2.3 部署前物料

- 新增并完善数据库初始化迁移（Alembic 初始版本）
- 新增种子数据脚本（可独立执行）
- 补齐生产环境变量模板
- 更新 docker-compose.prod.yml：后端容器启动时先迁移，再注入种子

### 2.4 验证结果

已完成以下验证：

1. backend 测试：通过（4/4）
2. Alembic upgrade：通过（SQLite 校验库）
3. 种子脚本执行：通过（SQLite 校验库）
4. frontend 构建：通过
5. docker compose 生产编排解析：通过

---

## 3. 本轮刻意未动范围（边界声明）

以下核心业务主实现仍归 B，本轮未代做：

- backend/app/services/task_service.py
- backend/app/services/wallet_service.py
- backend/app/services/credit_service.py
- backend/app/services/recommendation_service.py

以下前端业务页主责仍归 A，本轮未改写为完整业务实现：

- 大部分业务页面仍保留 A 主导落地节奏
- 地图页仅保持现有基线可联调，未越界改动 A 的页面主架构职责

---

## 4. 给 A/B 的直接协作说明

### 4.1 给 A（前端）

- 可基于已冻结地图字段直接对接楼宇渲染：latitude/longitude/polygon
- 可基于当前 WebSocket v1 事件集合做通知与聊天接入，不需要等待额外事件扩展
- 上传、认证、通知、聊天 HTTP 兜底、地图查询已具备联调入口

### 4.2 给 B（核心业务）

- 任务状态机、钱包、信用、推荐仍是 B 主线，不受本轮接口收口影响
- 可在当前基础设施上继续推进业务逻辑，不需要再等待认证/通知/审核/配置底座

---

## 5. 当前剩余风险与下一步

已知边界：

- 本机未完成真实 MySQL 联调启动（当前环境不可用）
- 数据库链路通过 SQLite 完成迁移与种子验证，可证明脚本与模型 wiring 可运行

建议下一步（在可用的 compose + MySQL 环境）：

1. 跑一次生产编排启动流程
2. 执行 alembic upgrade head
3. 执行种子注入
4. 进行 A/B 联调冒烟（认证、地图、通知、聊天兜底）

---

## 6. 结论

本轮已完成“只做 C-owner 份内”的部署前闭环：

- 契约收口完成
- 后端基础设施可运行
- 迁移与种子链路可验证
- C-owner 测试到位

同时保持了 A/B 的主责边界，保证后续联调可并行推进。
