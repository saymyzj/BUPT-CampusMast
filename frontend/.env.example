# -----------------------------------------------------------------------------
# 文件说明：
# 这是前端环境变量模板。A 同学联调前只需要把它复制成 .env，并根据本地或
# check 环境切换 API 地址与 WebSocket 地址即可。
# -----------------------------------------------------------------------------

VITE_APP_TITLE=CampusMast
VITE_API_BASE_URL=http://localhost:9000
VITE_WS_BASE_URL=ws://localhost:9000/ws
