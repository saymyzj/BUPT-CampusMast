# -----------------------------------------------------------------------------
# 文件说明：
# 这是本地验收版后端依赖清单。数据库结构由 SQLAlchemy 模型直接建表，
# 图片上传使用后端本地存储，DeepSeek 审核按环境变量可选启用。
# -----------------------------------------------------------------------------

fastapi==0.115.11
uvicorn[standard]==0.34.0
sqlalchemy==2.0.39
pymysql==1.1.1
redis==5.2.1
pydantic-settings==2.8.1
email-validator==2.2.0
python-jose==3.4.0
passlib[bcrypt]==1.7.4
httpx==0.28.1
pytest==8.3.5
python-multipart==0.0.20
